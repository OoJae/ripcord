# pnpm 11.1.x crashes in runDepsStatusCheck when running scripts (recursive spawn bug);
# scripts don't need the pre-run deps verification anyway — CI uses --frozen-lockfile.
verify-deps-before-run=false
